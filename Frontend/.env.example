# Copy to .env (local) or set in your deploy host's build env.
# Base URL of the Django backend. Leave unset for local dev (defaults to
# http://127.0.0.1:8000). For a deployed frontend, point this at your hosted
# backend, e.g. https://scsp-api.onrender.com
VITE_API_BASE_URL=http://127.0.0.1:8000
